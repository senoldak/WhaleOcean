# ==============================================================================
# Whale Ocean Environment Configuration Example
# ==============================================================================

# HTTP Port for Next.js web application (Default: 3000)
PORT=3000

# Path to the local SQLite database file (Default: ./whale_ocean.db)
SQLITE_DB_PATH=./whale_ocean.db

# Node environment mode: development | production | test
NODE_ENV=development
